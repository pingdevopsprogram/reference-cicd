dist: focal

# language: go

before_install:
  - sudo apt-get update
#   - sudo apt-get -y install libxml2-dev amazon-ecr-credential-helper

install: 
  - ./ci-scripts/install-dependencies.sh

jobs:
  include:
    - stage: "init"
      name: initialize
      script: ./ci-scripts/check-tools.sh
    - stage: deploy
      name: d.dev-helm
      script: ./ci-scripts/deploy-release.sh